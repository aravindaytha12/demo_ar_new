<table>
  <tr>
    <td>
      <%= link_to "Click and Change", {}, :onclick => "clickAndChange();", :remote => true %>
    </td>
    <td id="some_id">
      "old text needed to be changed"
    </td>
  </tr>
</table>
<script type="text/javascript">
  function clickAndChange(){
    var url_path = "/articles/update_text_method";
    $.ajax({
      type: 'GET'
      url: url_path,
      success: function(result){

      }
    });
  }
</script>
<%
  get 'articles/update_text_method' => 'articles#update_text_method'
  resources :articles  
  #articles_update_text_method_path
%>
<%
  def update_text_method
    @new_text = "This is new text, which is changed after onclick." 
  end 
%>
<script type"text/javscript>
  $("#some_Id").html("<%= @new_text %>");
  $("#some_id").html("<%= escape_javascript render(:partial => 'update_text_method_partial') %>")
</script>
<script>
  //= require jquery
  //= require jquery_ujs
</script>

in new partial file, if needed and if we want to call partial inside that 'escape_javascript'
"This is new text, changed after onclick of that link"